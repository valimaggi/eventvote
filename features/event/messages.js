const messages = require('../../common/messages');

module.exports = Object.assign(messages, {
  NONEXISTENT_DATES: 'Date(s) not found in the event',
  NO_SUITABLE_DATE: 'No suitable date found',
});
